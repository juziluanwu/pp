<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.pp.mapper.SysUserDao">
    <resultMap id="BaseResultMap" type="app.pp.entity.SysUserEntity">
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="salt" jdbcType="VARCHAR" property="salt"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="mobile" jdbcType="VARCHAR" property="mobile"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="create_user_id" jdbcType="INTEGER" property="createUserId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <!-- 查询用户的所有权限 -->
    <select id="queryAllPerms" resultType="string">
		select m.perms from wwls_sys_user_role ur
			LEFT JOIN wwls_sys_role_menu rm on ur.role_id = rm.role_id
			LEFT JOIN wwls_sys_menu m on rm.menu_id = m.menu_id
		where ur.user_id = #{userId}
	</select>

    <!-- 查询用户的所有菜单ID -->
    <select id="queryAllMenuId" resultType="long">
		select distinct rm.menu_id from wwls_sys_user_role ur
			LEFT JOIN wwls_sys_role_menu rm on ur.role_id = rm.role_id
		where ur.user_id = #{userId}
	</select>

    <select id="queryByUserName" resultType="app.pp.entity.SysUserEntity" resultMap="BaseResultMap">
		select * from wwls_sys_user where username = #{username}
	</select>
    <update id="updateById" parameterType="app.pp.entity.SysUserEntity">
        update wwls_sys_user
        <set>
            <if test="username != null and username != ''">
                username=#{username},
            </if>
            <if test="password!=null and password!=''">
                password=#{password},
            </if>
            <if test="salt!=null and salt!=''">
                salt=#{salt},
            </if>
            <if test="email!=null and email!=''">
                email=#{email},
            </if>
            <if test="mobile!=null and mobile!=''">
                mobile=#{mobile},
            </if>
            <if test="status!=null">
                status=#{status},
            </if>
            <if test="type!=null">
                type = #{type},
            </if>
            <if test="expats!=null">
                expats = #{expats},
            </if>
        </set>
        where user_id=#{userId}
    </update>
    <delete id="deleteBatchIds" parameterType="java.util.ArrayList">
        delete from wwls_sys_user where user_id in
        <foreach item="userId" collection="list" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <select id="selectByMap" resultMap="BaseResultMap">
        select u.* from wwls_sys_user u
        <where>
            1=1
            <if test="username!=null and username !=''">
                and  u.username like CONCAT('%',#{username},'%' )
            </if>
            <if test="createUserId!=null and createUserId !=''">
                and  u.create_user_id=#{createUserId}
            </if>
        </where>
        order by u.user_id desc
    </select>

    <update id="updatePassword">
        update wwls_sys_user set password=#{newPassword} where user_id=#{userId} and password=#{password}
    </update>

    <select id="selectById" resultMap="BaseResultMap" resultType="app.pp.entity.SysUserEntity">
        select * from wwls_sys_user where user_id=#{value}
    </select>
    <select id="selectPermissByUserId" resultType="string" parameterType="long">
		select m.perms from wwls_sys_user_role ur
			LEFT JOIN wwls_sys_role_menu rm on ur.role_id = rm.role_id
			LEFT JOIN wwls_sys_menu m on rm.menu_id = m.menu_id
		where m.type=2 and  ur.user_id = #{userId}
	</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="userId" parameterType="app.pp.entity.SysUserEntity">
    insert into wwls_sys_user(username,password,salt,email,mobile,status,type,create_user_id,create_time,expats)
    values(#{username},#{password},#{salt},#{email},#{mobile},#{status},#{type},#{createUserId},#{createTime},#{expats})

    </insert>

    <select id="selectByUsername" parameterType="String" resultMap="BaseResultMap">
        select * from wwls_sys_user where username = #{username}
    </select>
</mapper>